[data]
input_file = input/longman.definitions
output_dir = data/longman.sen.newest

[filter]
keep_multiword = False  # include MWEs
keep_apostrophes = False  # include words with apostrophes
first_only = False  # discard all but the first definition of each headword
